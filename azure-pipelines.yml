
trigger:
- main

jobs:
- job: myJob
  pool:
    name: Default
  steps:
    - checkout: self
      clean: false

    - task: privateTask@0
      inputs:
        samplestring: 'private'
      name: privateTask

    - task: greetTask@1
      inputs:
        samplestring: 'public'
        
    - bash: |
        echo $(privateTask.outputVar)

    - bash: |
        echo $(outputVar2)
      

- job: secondJob
  dependsOn: myJob
  variables:
    varFromMyJob: $[dependencies.myJob.outputs['privateTask.outputVar']]
  pool:
    name: Default
    
  # condition: eq(dependencies.myJob.outputs['privateTask.outputVar'], 'id')
  steps:
    - bash: |
        echo $(varFromMyJob) 
      
    
    
    
        
